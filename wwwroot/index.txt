@model IEnumerable<WebsiteBanHang.Models.Product>
<!-- Định nghĩa danh sách sản phẩm -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Đảm bảo giao diện phù hợp mọi thiết bị -->
    <title>Trang Web Bán Hàng</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"> <!-- Nhúng Bootstrap và Font Awesome -->
\    <link rel="stylesheet" href="~/css/nut-dang-nhap-dang-ky.css"> <!-- Style cho nút đăng nhập/đăng ký -->
    <link rel="stylesheet" href="~/css/hop-thoai-dang-xuat.css"> <!-- Style cho hộp thoại đăng xuất -->
    <link rel="stylesheet" href="~/css/mang-xa-hoi.css"> <!-- Style cho sidebar social media -->
    <link rel="stylesheet" href="~/css/thanh-tim-kiem.css"> <!-- Style cho thanh tìm kiếm -->
    <link rel="stylesheet" href="~/css/navbar_ThanhDieuHuong.css">
    <link rel="stylesheet" href="~/css/footer.css">
    <link rel="stylesheet" href="~/css/header_Tieu_De.css">
    <link rel="stylesheet" href="~/css/product_CardSanPham.css">
    <link rel="stylesheet" href="~/css/Bo_Cuc.css">
</head>
<body>
    <!-- Header: Full width -->
    <header class="bg-success text-white text-center py-4 full-width">
        <h1>Trang Web Bán Hàng</h1> <!-- Tiêu đề trang, màu xanh lá -->
    </header>

    <!-- Navbar: Full width -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-warning full-width">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Trang Chủ</a> <!-- Tên trang hoặc logo -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="#">Sản Phẩm</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Liên Hệ</a></li>
                </ul>

                <!-- Sử dụng class auth-button từ file nut-dang-nhap-dang-ky.css -->
                <ul class="navbar-nav auth-links">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li class="nav-item">
                            <a class="auth-button" href="/Identity/Account/Manage">Xin chào @ViewBag.HoTen!</a>
                        </li>
                        <li class="nav-item">
                            <form id="logoutForm" method="post" action="/Identity/Account/Logout" style="display: none;">
                                @Html.AntiForgeryToken()
                            </form>
                            <a href="#" id="logoutButton" class="auth-button logout-button">Đăng xuất</a>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item">
                            <a class="auth-button" href="/Identity/Account/Register">Đăng ký</a>
                        </li>
                        <li class="nav-item">
                            <a class="auth-button" href="/Identity/Account/Login">Đăng nhập</a>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </nav>

    <!-- Nội dung chính: Full width -->

    <div class="container-fluid mt-4">
        <!-- Khu vực tìm kiếm -->
        <div class="row mb-3">
            <div class="col-md-6">
                <form asp-action="Index" asp-controller="Home" method="get" class="search-bar">
                    <div class="input-group">
                        <input type="text" id="nhapTuKhoa" name="tuKhoaTimKiem" class="form-control" placeholder="Tìm kiếm sản phẩm..." value="@ViewData["tuKhoaTimKiem"]" autocomplete="off" />
                        <button type="submit" class="btn btn-primary">Tìm</button>
                    </div>
                    <div id="danhSachGoiY" class="dropdown-menu"></div>
                </form>
            </div>
        </div>

        <!-- Thông báo khi không có kết quả -->
        @if (ViewBag.KetQuaTimKiem != null)
        {
            <p class="text-danger">@ViewBag.KetQuaTimKiem</p>
            <h4>Gợi ý sản phẩm</h4>
        }

        <div class="row">
            <!-- Danh sách sản phẩm: Tăng chiều rộng lên 10/12 -->
            <div class="col-md-10">
                <div class="row row-cols-1 row-cols-md-4 g-4">
                    @if (Model == null || !Model.Any())
                    {
                        <p class="text-center">Không có sản phẩm nào để hiển thị.</p>
                    }
                    else
                    {
                        @foreach (var product in Model)
                        {
                            <div class="col">
                                <div class="card">
                                    <a asp-action="ProductDetail" asp-controller="Home" asp-route-id="@product.Id">
                                        <img src="@product.ImageUrl" class="card-img-top" alt="@product.Name">
                                    </a>
                                    <div class="card-body">
                                        <a asp-action="ProductDetail" asp-controller="Home" asp-route-id="@product.Id" style="text-decoration: none;">
                                            <h3 class="card-title">@product.Name</h3>
                                        </a>
                                        <p class="card-text">@(product.Description ?? "Không có mô tả")</p>
                                        <p class="price">@product.Price.ToString("N0") VND</p>
                                        <div class="btn-group">
                                            <a href="@Url.Action("ThanhToan", "HoaDon", new { productId = product.Id })" class="btn btn-primary">Mua Ngay</a>
                                            <a href="@Url.Action("AddToCart", "Cart", new { productId = product.Id })" class="btn btn-warning">Thêm giỏ</a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    }
                </div>
            </div>
            <!-- Sidebar: Giảm xuống 2/12 -->
            <div class="col-md-2">
                <div class="social-media-container">
                    <h5>Social Media</h5>
                    <ul class="list-unstyled social-links">
                        <li><a href="#" class="social-link"><i class="fab fa-facebook-f"></i> Facebook</a></li>
                        <li><a href="#" class="social-link"><i class="fab fa-twitter"></i> Twitter</a></li>
                        <li><a href="#" class="social-link"><i class="fab fa-instagram"></i> Instagram</a></li>
                        <li><a href="#" class="social-link"><i class="fab fa-linkedin-in"></i> LinkedIn</a></li>
                        <li><a href="#" class="social-link"><i class="fab fa-whatsapp"></i> Zalo</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal xác nhận đăng xuất -->
    <div class="modal fade custom-modal" id="logoutModal" tabindex="-1" aria-labelledby="logoutModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content custom-modal-content">
                <div class="modal-header custom-modal-header">
                    <h5 class="modal-title custom-modal-title" id="logoutModalLabel">Xác nhận</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body custom-modal-body">
                    Bạn có muốn đăng xuất khỏi tài khoản không?
                </div>
                <div class="modal-footer custom-modal-footer">
                    <button type="button" class="btn btn-custom-modal" id="cancelBtn" data-bs-dismiss="modal">Hủy</button>
                    <button type="button" class="btn btn-custom-modal" id="confirmBtn">Xác nhận</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script> <!-- Nhúng jQuery để dùng AJAX -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script> <!-- Nhúng Bootstrap JS -->
    <script>
        $(document).ready(function () {
            const nhapTuKhoa = $("#nhapTuKhoa");
            const danhSachGoiY = $("#danhSachGoiY");

            nhapTuKhoa.on("input", function () {
                const tuKhoa = $(this).val().trim();
                if (tuKhoa.length > 0) {
                    $.ajax({
                        url: "@Url.Action("SearchSuggestions", "Product", new { area = "Admin" })",
                        type: "GET",
                        data: { term: tuKhoa },
                        success: function (danhSach) {
                            danhSachGoiY.empty();
                            if (danhSach.length > 0) {
                                danhSach.forEach(sanPham => {
                                    danhSachGoiY.append(`
                                        <a href="/Home/ProductDetail?id=${sanPham.id}" class="dropdown-item d-flex align-items-center">
                                            <img src="${sanPham.imageUrl}" alt="${sanPham.name}" style="width: 3rem; height: 3rem; margin-right: 0.6rem;" />
                                            <div>
                                                <strong>${sanPham.name}</strong><br/>
                                                <small>${sanPham.price.toLocaleString()} VND</small>
                                            </div>
                                        </a>
                                    `);
                                });
                                danhSachGoiY.addClass("show");
                            } else {
                                danhSachGoiY.removeClass("show");
                            }
                        },
                        error: function () {
                            danhSachGoiY.empty().removeClass("show");
                        }
                    });
                } else {
                    danhSachGoiY.empty().removeClass("show");
                }
            });

            $(document).click(function (e) {
                if (!nhapTuKhoa.is(e.target) && !danhSachGoiY.is(e.target) && danhSachGoiY.has(e.target).length === 0) {
                    danhSachGoiY.removeClass("show");
                }
            });

            // Đăng xuất với Bootstrap Modal
            const logoutButton = document.getElementById('logoutButton');
            const logoutModal = new bootstrap.Modal(document.getElementById('logoutModal'));
            const confirmBtn = document.getElementById('confirmBtn');
            const cancelBtn = document.getElementById('cancelBtn');

            if (logoutButton) {
                logoutButton.addEventListener('click', function(e) {
                    e.preventDefault();
                    logoutModal.show();
                });
            } else {
                console.error('Logout button not found!');
            }

            if (confirmBtn) {
                confirmBtn.addEventListener('click', function() {
                    $.ajax({
                        url: '/Identity/Account/Logout',
                        type: 'POST',
                        data: $('#logoutForm').serialize(),
                        success: function() {
                            location.reload();
                        },
                        error: function() {
                            alert('Đăng xuất thất bại. Vui lòng thử lại.');
                        }
                    });
                    logoutModal.hide();
                });
            }

            if (cancelBtn) {
                cancelBtn.addEventListener('click', function() {
                    logoutModal.hide();
                });
            }
        });
    </script>
</body>
</html>